{
  "mcpServers": {
    "convex": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "npx", "-y", "convex@latest", "mcp", "start"],
      "env": {}
    },
    "strapi": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "npx", "-y", "strapi-mcp"],
      "env": {
        "STRAPI_URL": "$STRAPI_API_URL",
        "STRAPI_API_TOKEN": "$STRAPI_API_SECRET"
      }
    }
  }
}
